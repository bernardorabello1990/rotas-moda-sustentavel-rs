---
import { site } from '../data/site.ts';
const ano = new Date().getFullYear();
---
<footer class="rodape">
  <div class="container">
    <p class="rodape__edital">{site.creditoEdital}</p>
    <!-- Selos/logos institucionais do edital entram aqui quando confirmados (pendência com a equipe) -->
    <div class="rodape__linha">
      <span>© {ano} {site.nome}</span>
      <span>
        <a href={`mailto:${site.email}`}>{site.email}</a>
        {site.instagram && <> · <a href={site.instagram}>Instagram</a></>}
      </span>
    </div>
  </div>
</footer>

<style>
  .rodape {
    background: var(--cor-texto);
    color: var(--cor-fundo-alt);
    padding-block: 2.75rem;
    font-size: 0.9rem;
    /* Ourela: costura no topo do rodapé */
    border-top: 2px dashed color-mix(in srgb, var(--cor-acento) 60%, transparent);
  }
  .rodape a { color: var(--cor-acento); text-decoration: none; }
  .rodape a:hover { text-decoration: underline; }
  .rodape__edital { margin-bottom: 1.6rem; max-width: 46rem; }
  .rodape__linha {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: 0.5rem 1.5rem;
    border-top: 1.5px dashed color-mix(in srgb, var(--cor-fundo) 24%, transparent);
    padding-top: 1.3rem;
  }
</style>
